// Challenge 4: turn the array [3, 2, 1] into the string: "3 is bigger than 2 is bigger than 1" using the join method.

// Answer:
var array = [3,2,1];

// displaying on screen
console.log('Array:', array);
console.log(array.join(' is bigger than '));
